set :application, "com"
set :repository,  "git@github.com:hideyukisaito/#{application}.git"

set :user,        "hideyukisaito"
set :password,    "heeeeeee0729"

set :scm,         :git
set :branch,      "master"
set :deploy_via,  :copy
set :deploy_to,   "/var/app/#{application}"
# Or: `accurev`, `bzr`, `cvs`, `darcs`, `git`, `mercurial`, `perforce`, `subversion` or `none`

role :web, "49.212.40.241"                          # Your HTTP server, Apache/etc
role :app, "49.212.40.241"                          # This may be the same as your `Web` server
role :db,  "49.212.40.241", :primary => true # This is where Rails migrations will run

# if you're still using the script/reaper helper you will need
# these http://github.com/rails/irs_process_scripts
# If you are using Passenger mod_rails uncomment this:
# namespace :deploy do
#   task :start do ; end
#   task :stop do ; end
#   task :restart, :roles => :app, :except => { :no_release => true } do
#     run "#{try_sudo} touch #{File.join(current_path,'tmp','restart.txt')}"
#   end
# end
